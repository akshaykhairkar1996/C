#include<stdio.h>
void main()
{
	int a,b,c;
	printf("Enter the value of a,b,c:\n");
	scanf("%d" "%d" "%d",&a,&b,&c);
	if(a>b && a>c)
	{
		printf("%d is biggest number\n ",a);
	}
	else if(b>a && b>c)
	{
		
		printf("%d is biggest number\n ",b);
	}
	else
		printf("%d is the biggest number\n",c);
}

	
